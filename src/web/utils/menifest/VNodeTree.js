/**
 * 一个VNodeTree对应一个静态页
 * 
 * @class VNodeTree
 */
class VNodeTree {

  constructor (count, tree, map) {
    this.count = count
    this.tree = tree
    this.map = map
  }

  /**
   * 解析json
   * 
   * @static
   * @param {object or string} json 
   * @memberof VNodeTree
   */
  static parse (json) {

  }

  toJson () {

  }

}

export default VNodeTree